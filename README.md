# Gerrymander Research

Utah redistricting research pipeline (GerryChain, topology analysis, MCMC chains).

This repository mirrors the local `Gerrymander-Research` project. Very large generated artifacts (`*.jsonl`, `*.parquet`, `*.pbf`, `cache/`) are gitignored because they exceed GitHub size limits; regenerate or copy them locally as needed.
